BACKEND DEPLOY / UPDATE
=======================

cd /home/gpemureqjaea/api.verthoni.com

git pull origin main
composer install --no-dev --optimize-autoloader
composer dump-autoload -o

php artisan optimize:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear
php artisan filament:clear-cached-components || true
php artisan livewire:discover || true

php artisan migrate --force
php artisan db:seed --class=AdminUserSeeder --force
php artisan db:seed --class=VertoniCatalogSeeder --force
php artisan filament:assets
php artisan storage:link

php artisan config:cache
php artisan route:cache
php artisan view:cache
chmod -R 775 storage bootstrap/cache

IMPORTANT:
- Do not commit .env.
- After Filament/Livewire resource changes, clear browser cache/cookies or hard refresh admin pages.
- If real products are already managed by the owner, do not run VertoniCatalogSeeder on every deploy.

FRONTEND DEPLOY / UPDATE
========================

cd /path/to/vertoni-frontend

git pull origin main
npm ci
npm run generate
# upload .output/public to /home/gpemureqjaea/public_html
# make sure .htaccess is uploaded too
